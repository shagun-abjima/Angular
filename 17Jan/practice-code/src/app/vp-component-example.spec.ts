import { VpComponentExample } from './vp-component-example';

describe('VpComponentExample', () => {
  it('should create an instance', () => {
    expect(new VpComponentExample()).toBeTruthy();
  });
});
